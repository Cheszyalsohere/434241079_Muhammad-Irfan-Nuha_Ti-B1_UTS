/// Material 3 theme definitions (light + dark) with custom `ColorScheme`.
///
/// TODO(Phase 1): Build `ThemeData` with primary `#2563EB`, secondary
/// `#10B981`, error `#EF4444`, and typography from [AppTextStyles].
library;
