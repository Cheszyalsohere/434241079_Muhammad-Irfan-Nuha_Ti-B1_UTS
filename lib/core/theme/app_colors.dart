/// Brand colors + status/priority color tokens.
///
/// TODO(Phase 1): Define primary/secondary/error plus status map
/// (open=blue, in_progress=orange, resolved=green, closed=grey) and
/// priority map (low=grey, medium=blue, high=orange, urgent=red).
library;
