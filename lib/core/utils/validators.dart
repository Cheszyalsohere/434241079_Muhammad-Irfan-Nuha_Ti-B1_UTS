/// Form validators used across auth and ticket forms.
///
/// TODO(Phase 1): Email, required, min/max length, password strength,
/// attachment size limit. Return `String?` (null when valid) for
/// compatibility with `TextFormField.validator`.
library;
