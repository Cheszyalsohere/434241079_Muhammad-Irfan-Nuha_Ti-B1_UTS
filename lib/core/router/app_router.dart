/// `go_router` configuration with auth-aware redirect.
///
/// TODO(Phase 1): Declare routes (splash, login, register, reset,
/// dashboard, tickets, detail, create, notifications, profile) and
/// wire `refreshListenable` to Supabase auth stream so unauthenticated
/// users are redirected to /login.
library;
