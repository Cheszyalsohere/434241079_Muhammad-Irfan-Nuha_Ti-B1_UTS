/// Configured `Dio` instance for HTTP calls outside Supabase (if needed).
///
/// TODO(Phase 1): Set base options, timeouts, logging interceptor via
/// `logger`, and auth-token interceptor that reads from secure storage.
library;
