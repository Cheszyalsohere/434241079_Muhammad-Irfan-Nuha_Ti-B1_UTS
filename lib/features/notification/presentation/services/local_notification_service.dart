/// Wrapper around `flutter_local_notifications` — Android channel setup
/// and `show(...)` helpers.
///
/// TODO(Phase 5): Requires `POST_NOTIFICATIONS` runtime permission on
/// Android 13+ via `permission_handler`.
library;
