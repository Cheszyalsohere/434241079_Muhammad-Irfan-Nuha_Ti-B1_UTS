/// Implementation of [AuthRepository] — delegates to
/// [AuthRemoteDatasource] and maps exceptions to [Failure].
///
/// TODO(Phase 2): Implement each auth method returning
/// `Either<Failure, T>`.
library;
