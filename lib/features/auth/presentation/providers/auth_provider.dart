/// Riverpod `@riverpod` providers for auth state + operations.
///
/// TODO(Phase 2): Expose `authStateProvider`, `currentUserProvider`, and
/// mutation providers (login/register/logout/reset).
library;
