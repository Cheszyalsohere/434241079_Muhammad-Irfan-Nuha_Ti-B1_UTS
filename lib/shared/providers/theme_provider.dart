/// Riverpod `@riverpod` provider for the app's `ThemeMode`.
///
/// TODO(Phase 1): Persist selection to `shared_preferences` and expose
/// `setThemeMode` to switch between light / dark / system.
library;
